export const LIMITS = Object.freeze({
  maxMessageBytes: 32768,
  maxEnvelopeBytes: 120000,
  maxTextLength: 6000,
  maxSectionTextLength: 2500,
  maxTitleLength: 180,
  maxExamples: 4,
  maxOptions: 12,
  maxCodeLength: 8000,
  maxConstraints: 20,
  maxCandidateRegions: 12,
  maxWarnings: 12,
  duplicateWindowSize: 60,
})
